"""Runnable example apps shipped with edgevox.

These live inside the package so ``pip install edgevox`` is enough to
run them. ``edgevox.examples.agents`` collects the tool-calling demos
that back the ``edgevox-agent`` CLI.
"""
